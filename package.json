{
  "name": "stripe-prototype",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "typecheck": "tsc --noEmit",
    "dev": "echo 'Stage 1+: concurrently -k -n web,api,stripe \"bun --filter web dev\" \"bun --filter api dev\" \"stripe listen --forward-to localhost:8787/api/webhooks/stripe\"'"
  },
  "devDependencies": {
    "typescript": "^5.5.4"
  }
}
